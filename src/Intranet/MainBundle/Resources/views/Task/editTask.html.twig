<div class="row" id="popup">
	<div class="col-md-12">
		<div class="panel panel-cascade" style="width:600px;">
			<div class="panel-body">
            	<form action="{{ url('intranet_task_edit', {office_id: office.getId(), task_id: task.getId()}) }}" class="form-horizontal" role="form" method="post">
            		<div class="form-group">
                		<label for="inputEmail1" class="col-lg-2 col-md-3 control-label">Name</label>
                		<div class="col-lg-10 col-md-9">
                 			<input type="text" name="name" class="form-control form-cascade-control" id="inputEmail1" placeholder="Type task name here" value="{{ task.getName() }}" required>
               			</div>
             		</div>
             		<div class="form-group">
						  <label for="inputEmail1" class="col-lg-2 col-md-3 control-label">Priority</label>
						  <div class="col-lg-10 col-md-9">
						   <select name="priority" class="form-control">
						    <option value="high" {% if task.getPriority() == 'high' %}{{ 'selected' }}{% endif %}>High</option>
						    <option value="medium" {% if task.getPriority() == 'medium' %}{{ 'selected' }}{% endif %}>Medium</option>
						    <option value="low" {% if task.getPriority() == 'low' %}{{ 'selected' }}{% endif %}>Low</option>
						   </select>
						  </div>
					</div>
					<div class="form-group">
						  <label for="inputEmail1" class="col-lg-2 col-md-3 control-label">Status</label>
						  <div class="col-lg-10 col-md-9">
						   <select name="status" class="form-control">
						    <option value="pending" {% if task.getStatus() == 'pending' %}{{ 'selected' }}{% endif %}>Pending</option>
						    <option value="opened" {% if task.getStatus() == 'opened' %}{{ 'selected' }}{% endif %}>Opened</option>
						    <option value="re-opened" {% if task.getStatus() == 're-opened' %}{{ 'selected' }}{% endif %}>Re-opened</option>
						    <option value="in-progress" {% if task.getStatus() == 'in-progress' %}{{ 'selected' }}{% endif %}>In-progress</option>
						    <option value="resolved" {% if task.getStatus() == 'resolved' %}{{ 'selected' }}{% endif %}>Resolved</option>
						    <option value="testing" {% if task.getStatus() == 'testing' %}{{ 'selected' }}{% endif %}>Testing</option>
						    <option value="closed" {% if task.getStatus() == 'closed' %}{{ 'selected' }}{% endif %}>Closed</option>
						   </select>
						  </div>
					</div>
             		<div class="form-group">
  						<label for="inputEmail1" class="col-lg-2 col-md-3 control-label">Members</label>
  						<div class="col-lg-10 col-md-9">
   							<select name="members[]" multiple class="form-control">
   								{% for user in users %}
   								<option value="{{ user.getId() }}" {% if task.hasUser(user) %}{{ 'selected' }}{% endif %}>{{ user.getSurname() ~ ' ' ~ user.getName() ~ ' (' ~ user.getUsername() ~ ')' }}</option>
   								{% endfor %}	
  							</select>
						</div>
					</div>
					{% if topic is not defined %}
					<div class="form-group">
  						<label for="inputEmail1" class="col-lg-2 col-md-3 control-label">Topics</label>
  						<div class="col-lg-10 col-md-9">
   							<select name="topics[]" multiple class="form-control">
   								{% for topic in topics %}
   								<option value="{{ topic.getId() }}" {% if task.hasTopic(topic) %}{{ 'selected' }}{% endif %}>{{ topic.getName() }}</option>
   								{% endfor %}
  							</select>
						</div>
					</div>
					{% endif %}
             		<input type="hidden" name="backUrl" value="{{ backUrl }}">
             		{% if topic is defined %}
             		<input type="hidden" name="topicid" value="{{ topic.getId() }}">
             		{% endif %}
             		<div class="form-actions">
             			<input type="submit" value="Save Task" class="btn bg-primary btn-block text-white">
             		</div>
            	</form>
			</div>
		</div>
	</div>
</div>